# Nixpacks config for Railway — React/Vite frontend
# Builds the Vite app then serves with a static file server

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve dist -s -l tcp://0.0.0.0:${PORT:-3000}"
