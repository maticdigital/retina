# Retina API — lightweight dependencies for Vercel deployment
# Excludes heavy dependencies like weasyprint and playwright

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Data & config
pydantic>=2.6,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0,<2.0
pyyaml>=6.0,<7.0
email-validator>=2.0,<3.0

# HTTP client
httpx>=0.27,<1.0

# AI / LLM
anthropic>=0.40,<1.0

# Basic image processing (lighter than full PIL)
pillow>=10.0

# Supabase
supabase>=2.0

# Template rendering (for basic HTML, not PDF)
jinja2>=3.1,<4.0
