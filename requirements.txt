# Retina API — full dependencies for Railway deployment

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Data & config
pydantic>=2.6,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0,<2.0
pyyaml>=6.0,<7.0
email-validator>=2.0,<3.0

# HTTP client
httpx>=0.27,<1.0

# AI / LLM
anthropic>=0.40,<1.0

# Image processing
pillow>=10.0

# Supabase and dependencies
supabase>=2.0
gotrue>=2.0
postgrest>=0.10
realtime>=1.0
storage3>=0.5

# Template rendering
jinja2>=3.1,<4.0

# Password hashing
bcrypt>=4.0

# PDF generation (Railway has enough space for these)
weasyprint>=62.0
